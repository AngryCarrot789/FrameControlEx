using ColorPicker;
using FrameControlEx.Core.Settings.ColourTheme;

namespace FrameControlEx.Settings.ColourTheme {
    public class ColourSelector : IColourSelector {
        public Colour SelectARGB(Colour def = default) {
            StandardColorPicker picker = new StandardColorPicker();


            return default;
        }
    }
}